# ============================================================================
# Medical Chatbot with ReAct Architecture - Requirements
# ============================================================================

# -----------------------------------------------------------------------------
# Web Framework
# -----------------------------------------------------------------------------
flask==3.0.3
python-dotenv==1.0.1
gunicorn==22.0.0

# -----------------------------------------------------------------------------
# LangChain Ecosystem (Core ReAct Components)
# -----------------------------------------------------------------------------
langchain==0.2.14
langchain-community==0.2.12
langgraph==0.2.19                    # ReAct graph orchestration
langchain-pinecone==0.1.3
langchain-huggingface==0.0.3         # For HuggingFace embeddings
langchain-groq==0.1.9                # Groq cloud LLM

# -----------------------------------------------------------------------------
# Vector Database
# -----------------------------------------------------------------------------
pinecone-client==5.0.1

# -----------------------------------------------------------------------------
# Web Search Tools
# -----------------------------------------------------------------------------
duckduckgo-search==6.1.7
lxml                                 # Required for html/lite backends in duckduckgo-search
# Note: 'ddgs' is included in duckduckgo-search, no separate install needed

# -----------------------------------------------------------------------------
# ML Models & Embeddings
# -----------------------------------------------------------------------------
sentence-transformers==3.0.1         # Embeddings + CrossEncoder reranker
transformers>=4.30.0                 # Required by sentence-transformers
# torch is installed separately in Dockerfile with CPU-only build to keep image size small

# -----------------------------------------------------------------------------
# Local LLM Support (Removed - using Groq cloud API instead)
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
numpy>=1.24.0                        # Array operations
typing-extensions>=4.5.0             # Type hints for older Python

# -----------------------------------------------------------------------------
# Optional: Development Tools (uncomment if needed)
# -----------------------------------------------------------------------------
# pytest==8.0.0                      # Testing
# black==24.0.0                      # Code formatting
# flake8==7.0.0                      # Linting
# jupyter==1.0.0                     # Notebooks for experiments

